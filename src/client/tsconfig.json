{
  // Extends the base configuration from the specified path
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Enables the new JSX transform in React 17+
    "jsx": "react-jsx"
  }
}
